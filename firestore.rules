rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Anyone can read the doctor's current status (public page).
    // Only a signed-in staff account can change it (admin page).
    match /status/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
